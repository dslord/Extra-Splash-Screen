# Extra-Splash-Screen
A customizable splash screen inspired by the Windows login experience. It remains visible for a user-defined duration, allowing all startup applications and background services to load completely before the main interface is displayed.
